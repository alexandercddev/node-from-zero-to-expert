/**
 * @author: AlexanderCDs
 * @description: Template string
 * @date: 27/Julio/2021
 */

const name = 'Deadpool';
const real = 'Wade Winston';

const normal = name + ' ' + real;
const template = `${name} ${real}`; // template string

console.log(normal);
console.log(template);

console.log(normal === template)

const html = `
<h1>Hola</h1>
<p>Mundo</p>
`;

console.log(html)